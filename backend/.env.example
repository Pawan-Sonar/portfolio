# MongoDB
MONGO_URL=mongodb://localhost:27017
DB_NAME=portfolio_db

# CORS — comma-separated origins (use http://localhost:3000 for dev, your domain in prod)
CORS_ORIGINS=*

# OPTIONAL — Resend email (contact form forwarding)
# Get from https://resend.com  →  API Keys
RESEND_API_KEY=
SENDER_EMAIL=onboarding@resend.dev
OWNER_EMAIL=pawansonar965@gmail.com

# OPTIONAL — required to list contact submissions via GET /api/contact
ADMIN_TOKEN=
